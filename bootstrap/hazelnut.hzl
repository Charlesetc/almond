# Hazelnut Compiler

puts "Length:" "testing".length

define zero? do x
  if (x == 0) do
    return true
  end else do
    return false
  end
end

puts (zero? 10) (zero? 0)

(2).times do
  puts "iterating"
end

a = list 0
a.push 2
a.push 3

puts a
